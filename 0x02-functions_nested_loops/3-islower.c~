#include "main.h"
/**
 * _islower(int c) - check if char is lowercase
 * @c - check character
 * Return: 1 if char is lowercase, else 0
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
