#include "main.h"
/**
 * puts2 - prints every other character of a string
 * starting with the first character
 * @str: string
 * Return: output
 */
void puts2(char *str)
{
int write = 0;
while (str[write] != '\0')
write += 2;
}
