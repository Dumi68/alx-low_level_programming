#include "main.h"
/**
 * string_toupper - change all lowercase to uppercase
 * @p: pointer
 * Return: p
 */
char *string_toupper(char *n)
{
int x;
x = 0;
while (p[x] != '\0')
{
if (p[x] >= 'a' && p[x] <= 'z')
p[x] = p[x] - 32;
x++;
}
return (p);
}
